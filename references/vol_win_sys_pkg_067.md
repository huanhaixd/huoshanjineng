
## 火山"视窗项目"系统类库模块: 数据处理支持1

### 包: 火山.视窗.数据操作支持

---

#### 火山.视窗.数据操作支持.树结构数据类

* **类型:** `类`
* **名称:** `树结构数据类`
* **特性:**
	* `未公开`(无法在所处包外部使用,其中的内容只能通过此类的继承类对外提供)
	* `抽象类`(禁止直接定义其实例变量)
* **文档分类:** `辅助类`

##### 火山.视窗.数据操作支持.树结构数据类.树结构句柄

* **类型:** `成员变量`
* **名称:** `树结构句柄`
* **数据类型:** `变整数`

---

#### 火山.视窗.数据操作支持.树结构类

* **类型:** `类`
* **名称:** `树结构类`
* **模板基础类:** `参考对象模板类`
* **特性:**
	* `模板实现类`. 其直接/间接继承的模板基础类中的模板数据类型将按下表进行替换: `模板类型1` -> `树结构数据类`
* **描述:** 本类可生成并操作树型结构数据,可以理解为没有界面的树型框.
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.创建

* **类型:** `方法`
* **名称:** `创建`
* **描述:** 创建一个树结构数据
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.关闭

* **类型:** `方法`
* **名称:** `关闭`
* **描述:** 关闭树结构数据

##### 火山.视窗.数据操作支持.树结构类.取根节点

* **类型:** `方法`
* **名称:** `取根节点`
* **返回值数据类型:** `变整数`
* **描述:** 取当前树结构根节点
* **返回值描述:** 返回根节点句柄
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.置文本

* **类型:** `方法`
* **名称:** `置文本`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
	* `文本内容` (`文本型`): 提供要置入的文本内容
* **描述:** 给指定的树结构节点置入文本内容
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.取文本

* **类型:** `方法`
* **名称:** `取文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
* **描述:** 取出指定树节点的文本
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.取子节点数量

* **类型:** `方法`
* **名称:** `取子节点数量`
* **返回值数据类型:** `整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
* **描述:** 取出指定树结构的子节点数量
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.取子节点

* **类型:** `方法`
* **名称:** `取子节点`
* **返回值数据类型:** `变整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
	* `子节点索引` (`整数`): 提供子节点索引从0开始
* **描述:** 取出某树节点下的某个子节点句柄
* **返回值描述:** 返回子节点句柄
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.插入子节点

* **类型:** `方法`
* **名称:** `插入子节点`
* **返回值数据类型:** `变整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
	* `插入位置` (`整数`): 提供插入位置,从0开始.
	* `新节点内容` (`文本型`, 默认值: `""`): 提供新节点内容
* **描述:** 在指定树节点句柄处插入新的节点内容
* **返回值描述:** 返回插入后的新节点
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.添加子节点

* **类型:** `方法`
* **名称:** `添加子节点`
* **返回值数据类型:** `变整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
	* `子节点内容` (`文本型`): 提供要添加的节点内容
* **描述:** 给指定节点添加一个子节点
* **返回值描述:** 返回新增加的子节点句柄
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.删除子节点

* **类型:** `方法`
* **名称:** `删除子节点`
* **参数:**
	* `节点句柄` (`变整数`): 提供树节点句柄
	* `子节点索引` (`整数`): 提供要删除的子节点索引,从0开始
* **描述:** 删除某树节点下的某个子节点句柄
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.取父节点

* **类型:** `方法`
* **名称:** `取父节点`
* **返回值数据类型:** `变整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供任意树节点句柄
* **描述:** 通过任意节点取出此节点的父节点
* **返回值描述:** 返回父节点句柄

##### 火山.视窗.数据操作支持.树结构类.到字节集

* **类型:** `方法`
* **名称:** `到字节集`
* **返回值数据类型:** `字节集类`
* **参数:**
	* `节点句柄` (`变整数`): 提供树结构节点句柄
* **描述:** 将树结构转换成字节集
* **返回值描述:** 返回转换后的字节集

##### 火山.视窗.数据操作支持.树结构类.从字节集创建

* **类型:** `方法`
* **名称:** `从字节集创建`
* **参数:**
	* `节点句柄` (`变整数`): 提供树结构句柄
	* `数据` (`字节集类`): 提供字节集数据
* **描述:** 从字节集创建树结构,务必保证此字节集是通过"到字节集"方法保存的.

##### 火山.视窗.数据操作支持.树结构类.置附加数据1

* **类型:** `方法`
* **名称:** `置附加数据1`
* **参数:**
	* `节点句柄` (`变整数`): 提供节点句柄
	* `附加数据` (`变整数`): 提供要附加的数据
* **描述:** 给指定树节点句柄附加数据
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.置附加数据2

* **类型:** `方法`
* **名称:** `置附加数据2`
* **参数:**
	* `节点句柄` (`变整数`): 提供节点句柄
	* `附加数据` (`变整数`): 提供要附加的数据
* **描述:** 给指定树节点句柄附加数据

##### 火山.视窗.数据操作支持.树结构类.取附加数据1

* **类型:** `方法`
* **名称:** `取附加数据1`
* **返回值数据类型:** `变整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供节点句柄
* **描述:** 取出树节点设置的附加数据
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.取附加数据2

* **类型:** `方法`
* **名称:** `取附加数据2`
* **返回值数据类型:** `变整数`
* **参数:**
	* `节点句柄` (`变整数`): 提供节点句柄
* **描述:** 取出树节点设置的附加数据

##### 火山.视窗.数据操作支持.树结构类.到JSON对象

* **类型:** `方法`
* **名称:** `到JSON对象`
* **参数:**
	* `节点句柄` (`变整数`): 提供根节点句柄
	* `结果对象` (`JSON对象类`): 结果保存在此参数中
	* `JSON对象键名` (`文本型`, 默认值: `"对象"`): 提供对象键名
	* `JSON数组键名` (`文本型`, 默认值: `"数组"`): 提供数组键名
* **描述:** 将树结构转换成JSON对象数据
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)

##### 火山.视窗.数据操作支持.树结构类.从JSON对象创建

* **类型:** `方法`
* **名称:** `从JSON对象创建`
* **返回值数据类型:** `变整数`
* **参数:**
	* `要载入的对象` (`JSON对象类`): 提供要创建树结构的JSON对象,务必保证提供的对象是通过"树结构到JSON对象"方法保存的.
	* `JSON对象键名` (`文本型`, 默认值: `"对象"`): 提供对象键名
	* `JSON数组键名` (`文本型`, 默认值: `"数组"`): 提供数组键名
* **返回值描述:** 返回根节点句柄
* **相关例程:** [vol-树结构访问-treestruct-main](#vol-树结构访问-treestruct-main)
