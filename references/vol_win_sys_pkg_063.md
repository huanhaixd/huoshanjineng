
## 火山"视窗项目"系统类库模块: 增强位图操作

### 包: 火山.视窗.增强位图处理

---

#### 火山.视窗.增强位图处理.增强位图类

* **类型:** `类`
* **名称:** `增强位图类`
* **描述:** 处理位图操作
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main), [vol-二维码生成-qrencode-main](#vol-二维码生成-qrencode-main)

##### 火山.视窗.增强位图处理.增强位图类.是否存在

* **类型:** `属性读方法`
* **名称:** `是否存在`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 判断当前位图对象是否存在数据
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.宽度

* **类型:** `属性读方法`
* **名称:** `宽度`
* **返回值数据类型:** `整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 读取当前位图对象宽度
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.高度

* **类型:** `属性读方法`
* **名称:** `高度`
* **返回值数据类型:** `整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 读取当前位图对象高度
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.填充颜色

* **类型:** `属性写方法`
* **名称:** `填充颜色`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `填充颜色` (`整数`): 提供要填充的颜色
* **特性:**
	* `静态`
* **描述:** 给整个位图填充颜色

##### 火山.视窗.增强位图处理.增强位图类.颜色位数

* **类型:** `属性读方法`
* **名称:** `颜色位数`
* **返回值数据类型:** `整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 取出当前位图的颜色位数
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.从文件创建

* **类型:** `方法`
* **名称:** `从文件创建`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `本地文件路径` (`文本型`): 提供bmp位图文件路径
* **特性:**
	* `静态`
* **描述:** 从文件创建当前位图对象
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.从指针创建

* **类型:** `方法`
* **名称:** `从指针创建`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `位图文件指针` (`变整数`): 提供位图文件数据指针
	* `位图文件大小` (`整数`): 提供位图大小
* **特性:**
	* `静态`
* **描述:** 从指针创建位图对象
* **返回值描述:** 创建成功返回真

##### 火山.视窗.增强位图处理.增强位图类.从字节集创建

* **类型:** `方法`
* **名称:** `从字节集创建`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `位图字节集` (`字节集类`): 提供位图字节集数据
* **特性:**
	* `静态`
* **描述:** 从字节集创建位图对象
* **返回值描述:** 创建成功返回真

##### 火山.视窗.增强位图处理.增强位图类.从资源创建

* **类型:** `方法`
* **名称:** `从资源创建`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `资源` (`位图资源`): 提供位图资源
* **特性:**
	* `静态`
* **描述:** 从资源创建位图对象
* **返回值描述:** 创建成功返回真
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.取位图指针

* **类型:** `方法`
* **名称:** `取位图指针`
* **返回值数据类型:** `变整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指针
* **返回值描述:** 返回位图指针数据

##### 火山.视窗.增强位图处理.增强位图类.取位图大小

* **类型:** `方法`
* **名称:** `取位图大小`
* **返回值数据类型:** `整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 取当前位图对象大小
* **返回值描述:** 返回位图大小,单位字节
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.到字节集

* **类型:** `方法`
* **名称:** `到字节集`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果数据` (`字节集类`): 转换后的数据保存在此参数中
* **特性:**
	* `静态`
* **描述:** 将当前位图对象转换成字节集数据
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main), [vol-二维码生成-qrencode-main](#vol-二维码生成-qrencode-main)

##### 火山.视窗.增强位图处理.增强位图类.取像素数据缓冲指针

* **类型:** `方法`
* **名称:** `取像素数据缓冲指针`
* **返回值数据类型:** `变整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 24位位图像素数据是BGR排列,32位位图像素数据是BGRA排列
* **返回值描述:** 返回图像数据缓冲指针

##### 火山.视窗.增强位图处理.增强位图类.取像素数据数量

* **类型:** `方法`
* **名称:** `取像素数据数量`
* **返回值数据类型:** `整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
* **特性:**
	* `静态`
* **描述:** 取出位图像素数据数量

##### 火山.视窗.增强位图处理.增强位图类.取像素数据字节集

* **类型:** `方法`
* **名称:** `取像素数据字节集`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果字节集` (`字节集类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 取出位图像素数据字节集

##### 火山.视窗.增强位图处理.增强位图类.旋转

* **类型:** `方法`
* **名称:** `旋转`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `旋转度数` (`整数`): 填写旋转角度0-360度
	* `旋转后的图片` (`增强位图类`): 旋转后的位图保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对当前位图对象进行旋转操作
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.缩放

* **类型:** `方法`
* **名称:** `缩放`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `新宽度` (`整数`): 提供缩放后的新宽度
	* `新高度` (`整数`): 提供缩放后的新高度
	* `缩放后的图片` (`增强位图类`): 旋转后的位图保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对当前位图对象进行缩放操作
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.创建

* **类型:** `方法`
* **名称:** `创建`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `宽度` (`整数`): 提供要创建的位图宽度
	* `高度` (`整数`): 提供要创建的位图高度
	* `颜色数` (`整数`, 默认值: `24`): 可以创建24位位图，或者32位
* **特性:**
	* `静态`
* **描述:** 创建一个空的位图对象

##### 火山.视窗.增强位图处理.增强位图类.取坐标红色

* **类型:** `方法`
* **名称:** `取坐标红色`
* **返回值数据类型:** `字节`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指定坐标处的红色值

##### 火山.视窗.增强位图处理.增强位图类.取坐标绿色

* **类型:** `方法`
* **名称:** `取坐标绿色`
* **返回值数据类型:** `字节`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指定坐标处的绿色值

##### 火山.视窗.增强位图处理.增强位图类.取坐标蓝色

* **类型:** `方法`
* **名称:** `取坐标蓝色`
* **返回值数据类型:** `字节`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指定坐标处的蓝色值

##### 火山.视窗.增强位图处理.增强位图类.置坐标红色

* **类型:** `方法`
* **名称:** `置坐标红色`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
	* `颜色值` (`整数`): 提供新的颜色值
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指定坐标的红色值,并将红色修改成指定颜色值.

##### 火山.视窗.增强位图处理.增强位图类.置坐标绿色

* **类型:** `方法`
* **名称:** `置坐标绿色`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
	* `颜色值` (`整数`): 提供新的颜色值
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指定坐标的绿色值,并将红色修改成指定颜色值.

##### 火山.视窗.增强位图处理.增强位图类.置坐标蓝色

* **类型:** `方法`
* **名称:** `置坐标蓝色`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
	* `颜色值` (`整数`): 提供新的颜色值
* **特性:**
	* `静态`
* **描述:** 取当前位图对象指定坐标的蓝色值,并将红色修改成指定颜色值.

##### 火山.视窗.增强位图处理.增强位图类.取坐标颜色

* **类型:** `方法`
* **名称:** `取坐标颜色`
* **返回值数据类型:** `整数`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
* **特性:**
	* `静态`
* **描述:** 取指定坐标的颜色值

##### 火山.视窗.增强位图处理.增强位图类.置坐标颜色

* **类型:** `方法`
* **名称:** `置坐标颜色`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
	* `颜色值` (`整数`): 提供要设置的新颜色
* **特性:**
	* `静态`
* **描述:** 设置指定坐标的颜色值

##### 火山.视窗.增强位图处理.增强位图类.取红色通道数组

* **类型:** `方法`
* **名称:** `取红色通道数组`
* **参数:**
	* `结果数组` (`整数数组类`): 结果保存在此参数中
* **描述:** 取出所有的红色通道保存在数组中

##### 火山.视窗.增强位图处理.增强位图类.取绿色通道数组

* **类型:** `方法`
* **名称:** `取绿色通道数组`
* **参数:**
	* `结果数组` (`整数数组类`): 结果保存在此参数中
* **描述:** 取出所有的绿色通道保存在数组中

##### 火山.视窗.增强位图处理.增强位图类.取蓝色通道数组

* **类型:** `方法`
* **名称:** `取蓝色通道数组`
* **参数:**
	* `结果数组` (`整数数组类`): 结果保存在此参数中
* **描述:** 取出所有的蓝色通道保存在数组中

##### 火山.视窗.增强位图处理.增强位图类.取坐标像素黑白数组

* **类型:** `方法`
* **名称:** `取坐标像素黑白数组`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供横坐标
	* `纵坐标` (`整数`): 提供纵坐标
	* `数组` (`逻辑型数组类`): 结果保存到此参数
* **特性:**
	* `静态`
* **描述:** 取坐标周围像素的情况,为黑色则设置真,白色设置假. 将整个位图的所有像素的情况记录保存在逻辑数组中.

##### 火山.视窗.增强位图处理.增强位图类.亮度

* **类型:** `方法`
* **名称:** `亮度`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `亮度值` (`整数`): 范围-255到255
	* `结果位图` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 设置当前位图亮度
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.二值化

* **类型:** `方法`
* **名称:** `二值化`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `阈值` (`整数`): 范围0-255
	* `结果位图` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对当前位图进行二值化操作
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.灰度

* **类型:** `方法`
* **名称:** `灰度`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果位图` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对当前位图进行灰度处理
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.裁剪

* **类型:** `方法`
* **名称:** `裁剪`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `横坐标` (`整数`): 提供起始横坐标
	* `纵坐标` (`整数`): 提供起始纵坐标
	* `宽度` (`整数`): 提供要裁剪的宽度
	* `高度` (`整数`): 提供要裁剪的高度
	* `存放位图` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行裁剪操作
* **返回值描述:** 成功返回真
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.膨胀

* **类型:** `方法`
* **名称:** `膨胀`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果图片` (`增强位图类`): 返回膨胀后的位图
* **特性:**
	* `静态`
* **描述:** 对位图进行膨胀特效处理,准备膨胀的位图必须是黑白图片才能进行膨胀特效处理.
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.腐蚀

* **类型:** `方法`
* **名称:** `腐蚀`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对当前位图进行腐蚀特效处理,准备腐蚀的位图必须是黑白图片才能进行腐蚀特效处理.
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.马赛克

* **类型:** `方法`
* **名称:** `马赛克`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `强度` (`整数`): 从1开始,数值越大马赛克强度越大
	* `结果图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行马赛克处理
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.反色

* **类型:** `方法`
* **名称:** `反色`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行反色处理
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.中值滤波

* **类型:** `方法`
* **名称:** `中值滤波`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `滤波后的图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行中值滤波处理
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.均值滤波

* **类型:** `方法`
* **名称:** `均值滤波`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `滤波后的图片` (`增强位图类`): 处理结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行均值滤波处理
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.椒盐噪声

* **类型:** `方法`
* **名称:** `椒盐噪声`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `噪声数量` (`整数`): 数量越大噪点越多
	* `结果图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行椒盐噪声处理
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.轮廓

* **类型:** `方法`
* **名称:** `轮廓`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 取位图轮廓,只能取出字体类的位图轮廓,例如验证码,并且验证码必须进行二值化处理,才能使用.
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)

##### 火山.视窗.增强位图处理.增强位图类.细化

* **类型:** `方法`
* **名称:** `细化`
* **参数:**
	* `所操作的本对象` (`增强位图类`)
	* `结果图片` (`增强位图类`): 结果保存在此参数中
* **特性:**
	* `静态`
* **描述:** 对位图进行细化特效处理,准备细化的位图必须是黑白图片才能进行细化特效处理.主要针对人物,树木,验证码等操作处理.
* **相关例程:** [vol-位图处理-adv_bitmap-main](#vol-位图处理-adv_bitmap-main)
