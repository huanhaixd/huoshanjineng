
## 火山"视窗项目"系统类库模块: yyJSON

### 包: 火山视窗.JSON支持

---

#### 火山视窗.JSON支持.YYJSON只读对象类

* **类型:** `类`
* **名称:** `YYJSON只读对象类`
* **基础类:** `扩展对象类`
* **描述:** 本类仅用于解析功能,效率极高,如需使用构造功能请使用"YYJSON对象类".
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.创建自文本

* **类型:** `方法`
* **名称:** `创建自文本`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `内容` (`文本型`): 提供JSON内容文本
* **描述:** 从一段JSON文本创建一个对象
* **返回值描述:** 创建成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.创建自字节集

* **类型:** `方法`
* **名称:** `创建自字节集`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `内容` (`字节集类`): 请确保所提供内容为UTF8编码,且尾部不能带0结束符.
* **描述:** 从指定的内存从创建一个对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON只读对象类.创建自文件

* **类型:** `方法`
* **名称:** `创建自文件`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `路径` (`文本型`): 提供包含JSON内容文本的完整文件路径
* **描述:** 从包含一段JSON文本文件创建一个对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON只读对象类.取键值数

* **类型:** `方法`
* **名称:** `取键值数`
* **返回值数据类型:** `整数`
* **描述:** 获取当前JSON整个文档对象中的键值总数量(并非成员数).

##### 火山视窗.JSON支持.YYJSON只读对象类.取成员数

* **类型:** `方法`
* **名称:** `取成员数`
* **返回值数据类型:** `整数`
* **描述:** 获取当前JSON对象的成员数量

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为空

* **类型:** `方法`
* **名称:** `是否为空`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象是否为空(NULL)
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为真

* **类型:** `方法`
* **名称:** `是否为真`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为真

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为假

* **类型:** `方法`
* **名称:** `是否为假`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为假

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为逻辑型

* **类型:** `方法`
* **名称:** `是否为逻辑型`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为逻辑型
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为无符号整数

* **类型:** `方法`
* **名称:** `是否为无符号整数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为无符号整数类型

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为长整数

* **类型:** `方法`
* **名称:** `是否为长整数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为长整数类型

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为整数

* **类型:** `方法`
* **名称:** `是否为整数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为(包含有符号或无符号)整数

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为小数

* **类型:** `方法`
* **名称:** `是否为小数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为小数
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为数值

* **类型:** `方法`
* **名称:** `是否为数值`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为(包含有符号或无符号)整数或小数
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为文本

* **类型:** `方法`
* **名称:** `是否为文本`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为文本型
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为数组

* **类型:** `方法`
* **名称:** `是否为数组`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为数组

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为对象

* **类型:** `方法`
* **名称:** `是否为对象`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为对象

##### 火山视窗.JSON支持.YYJSON只读对象类.是否为容器

* **类型:** `方法`
* **名称:** `是否为容器`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为对象或者数组

##### 火山视窗.JSON支持.YYJSON只读对象类.取类型

* **类型:** `方法`
* **名称:** `取类型`
* **返回值数据类型:** `YYJSON值类型`
* **描述:** 获取当前对象值的具体类型

##### 火山视窗.JSON支持.YYJSON只读对象类.取子类型

* **类型:** `方法`
* **名称:** `取子类型`
* **返回值数据类型:** `YYJSON值子类型`
* **描述:** 获取当前对象值的子类型

##### 火山视窗.JSON支持.YYJSON只读对象类.取标签

* **类型:** `方法`
* **名称:** `取标签`
* **返回值数据类型:** `整数`
* **描述:** 获取当前对象值的标签

##### 火山视窗.JSON支持.YYJSON只读对象类.取类型描述

* **类型:** `方法`
* **名称:** `取类型描述`
* **返回值数据类型:** `文本型`
* **描述:** 获取类型名称文本

##### 火山视窗.JSON支持.YYJSON只读对象类.到数组

* **类型:** `方法`
* **名称:** `到数组`
* **返回值数据类型:** `YYJSON只读数组类`
* **描述:** 将当前对象作为数组类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.到逻辑值

* **类型:** `方法`
* **名称:** `到逻辑值`
* **返回值数据类型:** `逻辑型`
* **描述:** 将当前对象作为逻辑值类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.到长整数

* **类型:** `方法`
* **名称:** `到长整数`
* **返回值数据类型:** `长整数`
* **描述:** 将当前对象作为长整数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.到整数

* **类型:** `方法`
* **名称:** `到整数`
* **返回值数据类型:** `整数`
* **描述:** 将当前对象作为整数类型返回

##### 火山视窗.JSON支持.YYJSON只读对象类.到小数

* **类型:** `方法`
* **名称:** `到小数`
* **返回值数据类型:** `小数`
* **描述:** 将当前对象作为小数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.到文本

* **类型:** `方法`
* **名称:** `到文本`
* **返回值数据类型:** `文本型`
* **描述:** 将当前对象作为文本类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取逻辑值

* **类型:** `方法`
* **名称:** `取逻辑值`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以逻辑值类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径逻辑型

* **类型:** `方法`
* **名称:** `取路径逻辑型`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以逻辑值类型返回

##### 火山视窗.JSON支持.YYJSON只读对象类.取长整数

* **类型:** `方法`
* **名称:** `取长整数`
* **返回值数据类型:** `长整数`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以长整数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径长整数

* **类型:** `方法`
* **名称:** `取路径长整数`
* **返回值数据类型:** `长整数`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以长整数类型返回

##### 火山视窗.JSON支持.YYJSON只读对象类.取整数

* **类型:** `方法`
* **名称:** `取整数`
* **返回值数据类型:** `整数`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以整数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径整数

* **类型:** `方法`
* **名称:** `取路径整数`
* **返回值数据类型:** `整数`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以整数类型返回

##### 火山视窗.JSON支持.YYJSON只读对象类.取小数

* **类型:** `方法`
* **名称:** `取小数`
* **返回值数据类型:** `小数`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以小数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径小数

* **类型:** `方法`
* **名称:** `取路径小数`
* **返回值数据类型:** `小数`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以小数类型返回

##### 火山视窗.JSON支持.YYJSON只读对象类.取文本

* **类型:** `方法`
* **名称:** `取文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以文本类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径文本

* **类型:** `方法`
* **名称:** `取路径文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以文本类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径内容

* **类型:** `方法`
* **名称:** `取路径内容`
* **返回值数据类型:** `文本型`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 无论JSON类型是什么,都以文本数据返回.

##### 火山视窗.JSON支持.YYJSON只读对象类.取对象

* **类型:** `方法`
* **名称:** `取对象`
* **返回值数据类型:** `YYJSON只读对象类`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径对象

* **类型:** `方法`
* **名称:** `取路径对象`
* **返回值数据类型:** `YYJSON只读对象类`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值

##### 火山视窗.JSON支持.YYJSON只读对象类.取数组

* **类型:** `方法`
* **名称:** `取数组`
* **返回值数据类型:** `YYJSON只读数组类`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取数组值
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.取路径数组

* **类型:** `方法`
* **名称:** `取路径数组`
* **返回值数据类型:** `YYJSON只读数组类`
* **参数:**
	* `路径` (`文本型`): 例如:/user/0
* **描述:** 根据所提供的路径内容取数组值

##### 火山视窗.JSON支持.YYJSON只读对象类.到可读文本

* **类型:** `方法`
* **名称:** `到可读文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `选项` (`整数`, 默认值: `YYJSON格式化选项.缩进`): 请参考"YYJSON格式化选项"所提供的常量值,可使用其组合值.
* **描述:** 将当前对象以JSON格式化文本内容返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读对象类.枚举循环

* **类型:** `方法`
* **名称:** `枚举循环`
* **特性:**
	* 必须为本方法调用语句提供一个子语句体(其中可使用"跳出循环"和"到循环尾"关键字)
* **描述:** 启动一个循环来枚举当前对象中的每一个成员,在循环中可以调用"取枚举值"方法来获取当前所枚举到的成员值.

##### 火山视窗.JSON支持.YYJSON只读对象类.取枚举键对象

* **类型:** `方法`
* **名称:** `取枚举键对象`
* **返回值数据类型:** `YYJSON只读对象类`
* **特性:**
	* 本方法调用语句必须位于以下任一方法调用语句的直接或间接子语句体中: [枚举循环](#火山视窗.json支持.yyjson只读对象类.枚举循环)
* **描述:** 只能在本类的"枚举循环"语句的子语句体中使用,用作取回当前所枚举到的键值,如果有多层本类或本类继承类的"枚举循环"嵌套,本方法将返回最近层"枚举循环"所枚举到的键值.

##### 火山视窗.JSON支持.YYJSON只读对象类.取枚举值对象

* **类型:** `方法`
* **名称:** `取枚举值对象`
* **返回值数据类型:** `YYJSON只读对象类`
* **特性:**
	* 本方法调用语句必须位于以下任一方法调用语句的直接或间接子语句体中: [枚举循环](#火山视窗.json支持.yyjson只读对象类.枚举循环)
* **描述:** 只能在本类的"枚举循环"语句的子语句体中使用,用作取回当前所枚举到的成员值,如果有多层本类或本类继承类的"枚举循环"嵌套,本方法将返回最近层"枚举循环"所枚举到的成员值.

##### 火山视窗.JSON支持.YYJSON只读对象类.取展示内容

* **类型:** `方法`
* **名称:** `取展示内容`
* **参数:**
	* `展示内容` (`文本型`): 用作填入本对象中所有数据的展示文本
	* `最大展示数据尺寸` (`整数`): 提供用户所指定的最大允许展示数据尺寸,小于0表示全部展示,等于0表示展示默认尺寸数据.
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 将本对象中的所有数据用文本方式填入到所提供的文本参数中,用作调试或其它场合展示时使用.

##### 火山视窗.JSON支持.YYJSON只读对象类.流读入

* **类型:** `方法`
* **名称:** `流读入`
* **参数:**
	* `流对象` (`输入流类`): 提供欲从其中读入本对象数据内容的输入流
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 从所指定的流对象中读入本对象的数据. 请注意以下事项: 1. 一旦定义了"流读入"方法,必须定义对应的"流写出"方法,所读入内容格式与"流写出"方法写出的内容格式必须完全一致; 2. 任何类如果既没有定义"流读入"方法,也没有定义"流写出"方法,编译器会自动建立默认的"流读入"和"流写出"方法,默认方法就是首先调用基础类中的对应方法,然后将本对象中的所有非静态变量成员顺序从流中读入/写出; 3. 继承类中如果覆盖了本方法且基础类中也存在数据需要从流中读入,则必须负责调用基础类中的"流读入"方法,调用格式如: "父对象.流读入 (流对象)"; 4. 如果本方法中的读入操作比较复杂或耗时,在之前最好检查一下流对象当前是否处于出错状态("流对象.是否出错"方法返回真),如果出错则放弃后续的读入操作,以提高运行效率; 5. 在读入过程中一旦发现出错,则需要将对应的错误码写入"流对象.错误码"属性中(流读入出错不需要,会自动写入),以禁止后续的流读入操作. 错误码可以是"流错误码"类中的常量值,也可以是自行定义的错误码值(必须大于0); 6. 流对象一旦处于出错状态(流对象的"错误码"属性值不为"流错误码.无错误"),则后续所有流读入操作都会失败,如果欲再次使用此流对象,需要首先调用其"清除错误"方法清除掉流的出错状态或者将流关闭后再打开.

##### 火山视窗.JSON支持.YYJSON只读对象类.流写出

* **类型:** `方法`
* **名称:** `流写出`
* **参数:**
	* `流对象` (`输出流类`): 提供欲将本对象数据内容写出到其中的输出流
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 将本对象的数据写入所指定的流对象中. 注意: 1. 一旦定义了"流写出"方法,必须定义对应的"流读入"方法,所写出内容格式与"流读入"方法读取的内容格式必须完全一致; 2. 任何类如果既没有定义"流读入"方法,也没有定义"流写出"方法,编译器会自动建立默认的"流读入"和"流写出"方法,默认方法就是首先调用基础类中的对应方法,然后将本对象中的所有非静态变量成员顺序从流中读入/写出; 3. 继承类中如果覆盖了本方法且基础类中也存在数据需要写出到流中,则必须负责调用基础类中的"流写出"方法,调用格式如: "父对象.流写出 (流对象)"; 4. 如果本方法中的写出操作比较复杂或耗时,在之前最好检查一下流对象当前是否处于出错状态("流对象.是否出错"方法返回真),如果出错则放弃后续的写出操作,以提高运行效率; 5. 在写出过程中一旦发现出错,则需要将对应的错误码写入"流对象.错误码"属性中(流写出出错不需要,会自动写入),以禁止后续的流写出操作. 错误码可以是"流错误码"类中的常量值,也可以是自行定义的错误码值(必须大于0); 6. 流对象一旦处于出错状态(流对象的"错误码"属性值不为"流错误码.无错误"),则后续所有流写出操作都会失败,如果欲再次使用此流对象,需要首先调用其"清除错误"方法清除掉流的出错状态或者将流关闭后再打开.

---

#### 火山视窗.JSON支持.YYJSON只读数组类

* **类型:** `类`
* **名称:** `YYJSON只读数组类`
* **基础类:** `扩展对象类`
* **描述:** 本类仅用于解析功能,效率极高,如需使用构造功能请使用"YYJSON数组类".
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读数组类.取成员数

* **类型:** `方法`
* **名称:** `取成员数`
* **返回值数据类型:** `整数`
* **描述:** 获取该数组内包含的成员数量
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读数组类.取成员

* **类型:** `方法`
* **名称:** `取成员`
* **返回值数据类型:** `YYJSON只读对象类`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取逻辑值成员

* **类型:** `方法`
* **名称:** `取逻辑值成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取长整数

* **类型:** `方法`
* **名称:** `取长整数`
* **返回值数据类型:** `长整数`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取整数

* **类型:** `方法`
* **名称:** `取整数`
* **返回值数据类型:** `整数`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取小数

* **类型:** `方法`
* **名称:** `取小数`
* **返回值数据类型:** `小数`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取文本

* **类型:** `方法`
* **名称:** `取文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取首成员

* **类型:** `方法`
* **名称:** `取首成员`
* **返回值数据类型:** `YYJSON只读对象类`
* **描述:** 获取首个成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.取尾成员

* **类型:** `方法`
* **名称:** `取尾成员`
* **返回值数据类型:** `YYJSON只读对象类`
* **描述:** 获取最末尾处的成员值

##### 火山视窗.JSON支持.YYJSON只读数组类.枚举循环

* **类型:** `方法`
* **名称:** `枚举循环`
* **特性:**
	* 必须为本方法调用语句提供一个子语句体(其中可使用"跳出循环"和"到循环尾"关键字)
* **描述:** 启动一个循环来枚举当前数组中的每一个成员,在循环中可以调用"取枚举值"方法来获取当前所枚举到的成员值.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读数组类.取枚举值

* **类型:** `方法`
* **名称:** `取枚举值`
* **返回值数据类型:** `YYJSON只读对象类`
* **特性:**
	* 本方法调用语句必须位于以下任一方法调用语句的直接或间接子语句体中: [枚举循环](#火山视窗.json支持.yyjson只读数组类.枚举循环)
* **描述:** 只能在本类的"枚举循环"语句的子语句体中使用,用作取回当前所枚举到的成员值,如果有多层本类或本类继承类的"枚举循环"嵌套,本方法将返回最近层"枚举循环"所枚举到的成员值.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读数组类.到可读文本

* **类型:** `方法`
* **名称:** `到可读文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `选项` (`整数`, 默认值: `YYJSON格式化选项.缩进`): 请参考"YYJSON格式化选项"所提供的常量值,可使用其组合值.
* **描述:** 将当前数组以JSON格式化文本内容返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON只读数组类.取展示内容

* **类型:** `方法`
* **名称:** `取展示内容`
* **参数:**
	* `展示内容` (`文本型`): 用作填入本对象中所有数据的展示文本
	* `最大展示数据尺寸` (`整数`): 提供用户所指定的最大允许展示数据尺寸,小于0表示全部展示,等于0表示展示默认尺寸数据.
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 将本对象中的所有数据用文本方式填入到所提供的文本参数中,用作调试或其它场合展示时使用.

---

#### 火山视窗.JSON支持.YYJSON对象类

* **类型:** `类`
* **名称:** `YYJSON对象类`
* **基础类:** `扩展对象类`
* **描述:** 本类同时支持解析与构造,处理速度较慢,如仅需解析功能请使用"YYJSON只读对象类".
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.创建自文本

* **类型:** `方法`
* **名称:** `创建自文本`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `内容` (`文本型`): 提供JSON内容文本
* **描述:** 从一段JSON文本创建一个对象
* **返回值描述:** 创建成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.创建自字节集

* **类型:** `方法`
* **名称:** `创建自字节集`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `内容` (`字节集类`): 请确保所提供内容为UTF8编码,且尾部不能带0结束符.
* **描述:** 从指定的内存从创建一个对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自文件

* **类型:** `方法`
* **名称:** `创建值自文件`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `路径` (`文本型`): 提供包含JSON内容文本的完整文件路径
* **描述:** 从包含一段JSON文本文件创建一个对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自空值

* **类型:** `方法`
* **名称:** `创建值自空值`
* **返回值数据类型:** `逻辑型`
* **描述:** 根据指定的值创建一个单一值对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自逻辑值

* **类型:** `方法`
* **名称:** `创建值自逻辑值`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`逻辑型`)
* **描述:** 根据指定的值创建一个单一值对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自长整数

* **类型:** `方法`
* **名称:** `创建值自长整数`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`长整数`)
* **描述:** 根据指定的值创建一个单一值对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自整数

* **类型:** `方法`
* **名称:** `创建值自整数`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`整数`)
* **描述:** 根据指定的值创建一个单一值对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自小数

* **类型:** `方法`
* **名称:** `创建值自小数`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`小数`)
* **描述:** 根据指定的值创建一个单一值对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.创建值自文本

* **类型:** `方法`
* **名称:** `创建值自文本`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`文本型`)
* **描述:** 根据指定的值创建一个单一值对象
* **返回值描述:** 创建成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.是否为空

* **类型:** `方法`
* **名称:** `是否为空`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象是否为空(NULL)
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.是否为真

* **类型:** `方法`
* **名称:** `是否为真`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为真

##### 火山视窗.JSON支持.YYJSON对象类.是否为假

* **类型:** `方法`
* **名称:** `是否为假`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为假

##### 火山视窗.JSON支持.YYJSON对象类.是否为逻辑型

* **类型:** `方法`
* **名称:** `是否为逻辑型`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为逻辑型
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.是否为无符号整数

* **类型:** `方法`
* **名称:** `是否为无符号整数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为无符号整数类型

##### 火山视窗.JSON支持.YYJSON对象类.是否为长整数

* **类型:** `方法`
* **名称:** `是否为长整数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为长整数类型

##### 火山视窗.JSON支持.YYJSON对象类.是否为整数

* **类型:** `方法`
* **名称:** `是否为整数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为(包含有符号或无符号)整数

##### 火山视窗.JSON支持.YYJSON对象类.是否为小数

* **类型:** `方法`
* **名称:** `是否为小数`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为小数
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.是否为数值

* **类型:** `方法`
* **名称:** `是否为数值`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为(包含有符号或无符号)整数或小数
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.是否为文本

* **类型:** `方法`
* **名称:** `是否为文本`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为文本型
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.是否为数组

* **类型:** `方法`
* **名称:** `是否为数组`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为数组

##### 火山视窗.JSON支持.YYJSON对象类.是否为对象

* **类型:** `方法`
* **名称:** `是否为对象`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为对象

##### 火山视窗.JSON支持.YYJSON对象类.是否为容器

* **类型:** `方法`
* **名称:** `是否为容器`
* **返回值数据类型:** `逻辑型`
* **描述:** 检查当前对象的值是否为对象或者数组

##### 火山视窗.JSON支持.YYJSON对象类.取类型

* **类型:** `方法`
* **名称:** `取类型`
* **返回值数据类型:** `YYJSON值类型`
* **描述:** 获取当前对象值的具体类型

##### 火山视窗.JSON支持.YYJSON对象类.取子类型

* **类型:** `方法`
* **名称:** `取子类型`
* **返回值数据类型:** `YYJSON值子类型`
* **描述:** 获取当前对象值的子类型

##### 火山视窗.JSON支持.YYJSON对象类.取标签

* **类型:** `方法`
* **名称:** `取标签`
* **返回值数据类型:** `整数`
* **描述:** 获取当前对象值的标签

##### 火山视窗.JSON支持.YYJSON对象类.取类型描述

* **类型:** `方法`
* **名称:** `取类型描述`
* **返回值数据类型:** `文本型`
* **描述:** 获取类型名称文本

##### 火山视窗.JSON支持.YYJSON对象类.到数组

* **类型:** `方法`
* **名称:** `到数组`
* **返回值数据类型:** `YYJSON数组类`
* **描述:** 将当前对象作为数组类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.到逻辑值

* **类型:** `方法`
* **名称:** `到逻辑值`
* **返回值数据类型:** `逻辑型`
* **描述:** 将当前对象作为逻辑值类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.到长整数

* **类型:** `方法`
* **名称:** `到长整数`
* **返回值数据类型:** `长整数`
* **描述:** 将当前对象作为长整数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.到整数

* **类型:** `方法`
* **名称:** `到整数`
* **返回值数据类型:** `整数`
* **描述:** 将当前对象作为整数类型返回

##### 火山视窗.JSON支持.YYJSON对象类.到小数

* **类型:** `方法`
* **名称:** `到小数`
* **返回值数据类型:** `小数`
* **描述:** 将当前对象作为小数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.到文本

* **类型:** `方法`
* **名称:** `到文本`
* **返回值数据类型:** `文本型`
* **描述:** 将当前对象作为文本类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取逻辑值

* **类型:** `方法`
* **名称:** `取逻辑值`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以逻辑值类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径逻辑值

* **类型:** `方法`
* **名称:** `取路径逻辑值`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以逻辑值类型返回

##### 火山视窗.JSON支持.YYJSON对象类.取长整数

* **类型:** `方法`
* **名称:** `取长整数`
* **返回值数据类型:** `长整数`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以长整数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径长整数

* **类型:** `方法`
* **名称:** `取路径长整数`
* **返回值数据类型:** `长整数`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以长整数类型返回

##### 火山视窗.JSON支持.YYJSON对象类.取整数

* **类型:** `方法`
* **名称:** `取整数`
* **返回值数据类型:** `整数`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以整数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径整数

* **类型:** `方法`
* **名称:** `取路径整数`
* **返回值数据类型:** `整数`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以整数类型返回

##### 火山视窗.JSON支持.YYJSON对象类.取小数

* **类型:** `方法`
* **名称:** `取小数`
* **返回值数据类型:** `小数`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以小数类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径小数

* **类型:** `方法`
* **名称:** `取路径小数`
* **返回值数据类型:** `小数`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以小数类型返回

##### 火山视窗.JSON支持.YYJSON对象类.取文本

* **类型:** `方法`
* **名称:** `取文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值以文本类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径文本

* **类型:** `方法`
* **名称:** `取路径文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值并以文本类型返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径内容

* **类型:** `方法`
* **名称:** `取路径内容`
* **返回值数据类型:** `文本型`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 无论JSON类型是什么,都以文本数据返回.

##### 火山视窗.JSON支持.YYJSON对象类.取对象

* **类型:** `方法`
* **名称:** `取对象`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取对象值
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径对象

* **类型:** `方法`
* **名称:** `取路径对象`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `路径` (`文本型`): 路径必须以"/"开始,如:/user/0
* **描述:** 根据所提供的路径内容取对象值

##### 火山视窗.JSON支持.YYJSON对象类.取数组

* **类型:** `方法`
* **名称:** `取数组`
* **返回值数据类型:** `YYJSON数组类`
* **参数:**
	* `键名称` (`文本型`): 提供所欲获取其值对应的键名称
* **描述:** 根据指定键名称取数组值
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取路径数组

* **类型:** `方法`
* **名称:** `取路径数组`
* **返回值数据类型:** `YYJSON数组类`
* **参数:**
	* `路径` (`文本型`): 例如:/user/0
* **描述:** 根据所提供的路径内容取数组值

##### 火山视窗.JSON支持.YYJSON对象类.枚举循环

* **类型:** `方法`
* **名称:** `枚举循环`
* **特性:**
	* 必须为本方法调用语句提供一个子语句体(其中可使用"跳出循环"和"到循环尾"关键字)
* **描述:** 启动一个循环来枚举当前对象中的每一个成员,在循环中可以调用"取枚举值"方法来获取当前所枚举到的成员值.

##### 火山视窗.JSON支持.YYJSON对象类.取枚举键对象

* **类型:** `方法`
* **名称:** `取枚举键对象`
* **返回值数据类型:** `YYJSON对象类`
* **特性:**
	* 本方法调用语句必须位于以下任一方法调用语句的直接或间接子语句体中: [枚举循环](#火山视窗.json支持.yyjson对象类.枚举循环)
* **描述:** 只能在本类的"枚举循环"语句的子语句体中使用,用作取回当前所枚举到的成员值,如果有多层本类或本类继承类的"枚举循环"嵌套,本方法将返回最近层"枚举循环"所枚举到的成员值.

##### 火山视窗.JSON支持.YYJSON对象类.取枚举值对象

* **类型:** `方法`
* **名称:** `取枚举值对象`
* **返回值数据类型:** `YYJSON对象类`
* **特性:**
	* 本方法调用语句必须位于以下任一方法调用语句的直接或间接子语句体中: [枚举循环](#火山视窗.json支持.yyjson对象类.枚举循环)
* **描述:** 只能在本类的"枚举循环"语句的子语句体中使用,用作取回当前所枚举到的成员值,如果有多层本类或本类继承类的"枚举循环"嵌套,本方法将返回最近层"枚举循环"所枚举到的成员值.

##### 火山视窗.JSON支持.YYJSON对象类.取成员数

* **类型:** `方法`
* **名称:** `取成员数`
* **返回值数据类型:** `整数`
* **描述:** 获取当前JSON对象的成员数量

##### 火山视窗.JSON支持.YYJSON对象类.加入空成员

* **类型:** `方法`
* **名称:** `加入空成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
* **描述:** 在当前对象尾部加入新的空值(NULL)成员
* **返回值描述:** 加入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.加入逻辑值成员

* **类型:** `方法`
* **名称:** `加入逻辑值成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`逻辑型`): 提供所欲加入的值内容
* **描述:** 在当前对象尾部加入新的逻辑型成员
* **返回值描述:** 加入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.加入长整数成员

* **类型:** `方法`
* **名称:** `加入长整数成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`长整数`): 提供所欲加入的值内容
* **描述:** 在当前对象尾部加入新的长整数成员
* **返回值描述:** 加入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.加入整数成员

* **类型:** `方法`
* **名称:** `加入整数成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`整数`): 提供所欲加入的值内容
* **描述:** 在当前对象尾部加入新的整数成员
* **返回值描述:** 加入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.加入小数成员

* **类型:** `方法`
* **名称:** `加入小数成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`小数`): 提供所欲加入的值内容
* **描述:** 在当前对象尾部加入新的小数成员
* **返回值描述:** 加入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.加入文本成员

* **类型:** `方法`
* **名称:** `加入文本成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`文本型`): 提供所欲加入的值内容
* **描述:** 在当前对象尾部加入新的文本值成员
* **返回值描述:** 加入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.加入成员

* **类型:** `方法`
* **名称:** `加入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON对象类`): 提供所欲加入的成员值,必须确保该对象的作用域大于或等于当前对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
	* `覆盖` (`逻辑型`, 默认值: `假`): 如果当前对象中已存在该键,是否覆盖其值.
* **描述:** 插入一个指定的对象成员到当前对象的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.缓存并加入成员

* **类型:** `方法`
* **名称:** `缓存并加入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON对象类`): 提供所欲加入的成员值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被加入一次,多次被加入将导致成员值丢失或混乱.
	* `覆盖` (`逻辑型`, 默认值: `假`): 如果当前对象中已存在该键,是否覆盖其值.
* **描述:** 同"加入成员"方法,与其不同的是本方法参数的"值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.加入数组成员

* **类型:** `方法`
* **名称:** `加入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON数组类`): 提供所欲加入的成员值,必须确保该对象的作用域大于或等于当前对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
	* `覆盖` (`逻辑型`, 默认值: `假`): 如果当前对象中已存在该键,是否覆盖其值.
* **描述:** 插入一个指定的数组成员到当前对象的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.缓存加入数组成员

* **类型:** `方法`
* **名称:** `缓存加入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON数组类`): 提供所欲加入的成员值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被加入一次,多次被加入将导致成员值丢失或混乱.
	* `覆盖` (`逻辑型`, 默认值: `假`): 如果当前对象中已存在该键,是否覆盖其值.
* **描述:** 同"加入数组成员"方法,与其不同的是本方法参数的"值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.插入成员

* **类型:** `方法`
* **名称:** `插入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON对象类`): 提供所欲插入的成员值,必须确保该对象的作用域大于或等于当前对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 插入一个指定的对象成员到当前对象的指定索引位置
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.缓存并插入成员

* **类型:** `方法`
* **名称:** `缓存并插入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON对象类`): 提供所欲插入的成员值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被加入一次,多次被加入将导致成员值丢失或混乱.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 同"插入成员"方法,与其不同的是本方法参数的"值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.插入数组成员

* **类型:** `方法`
* **名称:** `插入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON数组类`): 提供所欲加入的成员值,必须确保该对象的作用域大于或等于当前对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 插入一个指定的对象成员到当前对象的指定索引位置
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.缓存并插入数组成员

* **类型:** `方法`
* **名称:** `缓存并插入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `值` (`YYJSON数组类`): 提供所欲加入的成员值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被加入一次,多次被加入将导致成员值丢失或混乱.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 同"插入数组成员"方法,与其不同的是本方法参数的"值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.替换成员

* **类型:** `方法`
* **名称:** `替换成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `替换值` (`YYJSON对象类`): 提供所欲替换的新值,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被替换一次,否则将出现成员值丢失的情况.
* **描述:** 将当前对象中指定的成员替换为一个新的值
* **返回值描述:** 替换成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.缓存并替换成员

* **类型:** `方法`
* **名称:** `缓存并替换成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `替换值` (`YYJSON对象类`): 提供所欲替换的新值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被替换一次,多次替换将导致成员值丢失或混乱.
* **描述:** 同"替换成员"方法,与其不同的是本方法参数的"替换值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 替换成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.替换数组成员

* **类型:** `方法`
* **名称:** `替换数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `替换值` (`YYJSON数组类`): 提供所欲替换的新值,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被替换一次,否则将出现成员值丢失的情况.
* **描述:** 将当前数组中指定的成员替换为一个新的值
* **返回值描述:** 替换成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.缓存并替换数组成员

* **类型:** `方法`
* **名称:** `缓存并替换数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
	* `替换值` (`YYJSON数组类`): 提供所欲替换的新值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被替换一次,多次替换将导致成员值丢失或混乱.
* **描述:** 同"替换数组成员"方法,与其不同的是本方法参数的"替换值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 替换成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.删除成员

* **类型:** `方法`
* **名称:** `删除成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `键名` (`文本型`): 提供所欲定义的键名称
* **描述:** 删除指定键所对应的所有值
* **返回值描述:** 删除成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON对象类.清空

* **类型:** `方法`
* **名称:** `清空`
* **返回值数据类型:** `逻辑型`
* **描述:** 清空当前对象中的所有成员
* **返回值描述:** 返回是否清空成功

##### 火山视窗.JSON支持.YYJSON对象类.清除缓存

* **类型:** `方法`
* **名称:** `清除缓存`
* **描述:** 在使用了"缓存并加入/插入"等系列方法后,如所被缓存对象不再需要使用(即充当当前对象的子对象)时,可调用本方法清理缓存所占用内存.

##### 火山视窗.JSON支持.YYJSON对象类.翻转

* **类型:** `方法`
* **名称:** `翻转`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `次数` (`整数`): 提供所欲翻转的次数
* **描述:** 将当前对象中的成员翻转指定次数
* **返回值描述:** 返回是否翻转成功

##### 火山视窗.JSON支持.YYJSON对象类.到可读文本

* **类型:** `方法`
* **名称:** `到可读文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `选项` (`整数`, 默认值: `YYJSON格式化选项.缩进`): 请参考"YYJSON格式化选项"所提供的常量值,可使用其组合值.
* **描述:** 将当前数组以JSON格式化文本内容返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON对象类.取展示内容

* **类型:** `方法`
* **名称:** `取展示内容`
* **参数:**
	* `展示内容` (`文本型`): 用作填入本对象中所有数据的展示文本
	* `最大展示数据尺寸` (`整数`): 提供用户所指定的最大允许展示数据尺寸,小于0表示全部展示,等于0表示展示默认尺寸数据.
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 将本对象中的所有数据用文本方式填入到所提供的文本参数中,用作调试或其它场合展示时使用.

##### 火山视窗.JSON支持.YYJSON对象类.流读入

* **类型:** `方法`
* **名称:** `流读入`
* **参数:**
	* `流对象` (`输入流类`): 提供欲从其中读入本对象数据内容的输入流
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 从所指定的流对象中读入本对象的数据. 请注意以下事项: 1. 一旦定义了"流读入"方法,必须定义对应的"流写出"方法,所读入内容格式与"流写出"方法写出的内容格式必须完全一致; 2. 任何类如果既没有定义"流读入"方法,也没有定义"流写出"方法,编译器会自动建立默认的"流读入"和"流写出"方法,默认方法就是首先调用基础类中的对应方法,然后将本对象中的所有非静态变量成员顺序从流中读入/写出; 3. 继承类中如果覆盖了本方法且基础类中也存在数据需要从流中读入,则必须负责调用基础类中的"流读入"方法,调用格式如: "父对象.流读入 (流对象)"; 4. 如果本方法中的读入操作比较复杂或耗时,在之前最好检查一下流对象当前是否处于出错状态("流对象.是否出错"方法返回真),如果出错则放弃后续的读入操作,以提高运行效率; 5. 在读入过程中一旦发现出错,则需要将对应的错误码写入"流对象.错误码"属性中(流读入出错不需要,会自动写入),以禁止后续的流读入操作. 错误码可以是"流错误码"类中的常量值,也可以是自行定义的错误码值(必须大于0); 6. 流对象一旦处于出错状态(流对象的"错误码"属性值不为"流错误码.无错误"),则后续所有流读入操作都会失败,如果欲再次使用此流对象,需要首先调用其"清除错误"方法清除掉流的出错状态或者将流关闭后再打开.

##### 火山视窗.JSON支持.YYJSON对象类.流写出

* **类型:** `方法`
* **名称:** `流写出`
* **参数:**
	* `流对象` (`输出流类`): 提供欲将本对象数据内容写出到其中的输出流
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 将本对象的数据写入所指定的流对象中. 注意: 1. 一旦定义了"流写出"方法,必须定义对应的"流读入"方法,所写出内容格式与"流读入"方法读取的内容格式必须完全一致; 2. 任何类如果既没有定义"流读入"方法,也没有定义"流写出"方法,编译器会自动建立默认的"流读入"和"流写出"方法,默认方法就是首先调用基础类中的对应方法,然后将本对象中的所有非静态变量成员顺序从流中读入/写出; 3. 继承类中如果覆盖了本方法且基础类中也存在数据需要写出到流中,则必须负责调用基础类中的"流写出"方法,调用格式如: "父对象.流写出 (流对象)"; 4. 如果本方法中的写出操作比较复杂或耗时,在之前最好检查一下流对象当前是否处于出错状态("流对象.是否出错"方法返回真),如果出错则放弃后续的写出操作,以提高运行效率; 5. 在写出过程中一旦发现出错,则需要将对应的错误码写入"流对象.错误码"属性中(流写出出错不需要,会自动写入),以禁止后续的流写出操作. 错误码可以是"流错误码"类中的常量值,也可以是自行定义的错误码值(必须大于0); 6. 流对象一旦处于出错状态(流对象的"错误码"属性值不为"流错误码.无错误"),则后续所有流写出操作都会失败,如果欲再次使用此流对象,需要首先调用其"清除错误"方法清除掉流的出错状态或者将流关闭后再打开.

---

#### 火山视窗.JSON支持.YYJSON数组类

* **类型:** `类`
* **名称:** `YYJSON数组类`
* **基础类:** `扩展对象类`
* **描述:** 本类同时支持解析与构造,处理速度较慢,如仅需解析功能请使用"YYJSON只读数组类".
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.创建自逻辑型数据

* **类型:** `方法`
* **名称:** `创建自逻辑型数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`逻辑型数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.创建自长整数数据

* **类型:** `方法`
* **名称:** `创建自长整数数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`长整数数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.创建自小数数据

* **类型:** `方法`
* **名称:** `创建自小数数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`小数数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.创建自字节数据

* **类型:** `方法`
* **名称:** `创建自字节数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`字节数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.创建自短整数数据

* **类型:** `方法`
* **名称:** `创建自短整数数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`短整数数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.创建自整数数据

* **类型:** `方法`
* **名称:** `创建自整数数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`整数数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.创建自文本数据

* **类型:** `方法`
* **名称:** `创建自文本数据`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `成员` (`文本数组类`): 提供所欲使用的成员数据
* **描述:** 根据所提供指定数量的成员创建本数组对象,如已创建成功则无法重复创建.
* **返回值描述:** 返回是否创建成功

##### 火山视窗.JSON支持.YYJSON数组类.取成员数

* **类型:** `方法`
* **名称:** `取成员数`
* **返回值数据类型:** `整数`
* **描述:** 获取该数组内包含的成员数量
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.取成员

* **类型:** `方法`
* **名称:** `取成员`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON数组类.取逻辑值成员

* **类型:** `方法`
* **名称:** `取逻辑值成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON数组类.取长整数

* **类型:** `方法`
* **名称:** `取长整数`
* **返回值数据类型:** `长整数`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON数组类.取整数

* **类型:** `方法`
* **名称:** `取整数`
* **返回值数据类型:** `整数`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON数组类.取小数

* **类型:** `方法`
* **名称:** `取小数`
* **返回值数据类型:** `小数`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON数组类.取文本

* **类型:** `方法`
* **名称:** `取文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `索引` (`整数`)
* **描述:** 获取指定索引位置的成员值

##### 火山视窗.JSON支持.YYJSON数组类.取首成员

* **类型:** `方法`
* **名称:** `取首成员`
* **返回值数据类型:** `YYJSON对象类`
* **描述:** 获取首个成员值

##### 火山视窗.JSON支持.YYJSON数组类.取尾成员

* **类型:** `方法`
* **名称:** `取尾成员`
* **返回值数据类型:** `YYJSON对象类`
* **描述:** 获取最末尾处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.枚举循环

* **类型:** `方法`
* **名称:** `枚举循环`
* **特性:**
	* 必须为本方法调用语句提供一个子语句体(其中可使用"跳出循环"和"到循环尾"关键字)
* **描述:** 启动一个循环来枚举当前数组中的每一个成员,在循环中可以调用"取枚举值"方法来获取当前所枚举到的成员值.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.取枚举值

* **类型:** `方法`
* **名称:** `取枚举值`
* **返回值数据类型:** `YYJSON对象类`
* **特性:**
	* 本方法调用语句必须位于以下任一方法调用语句的直接或间接子语句体中: [枚举循环](#火山视窗.json支持.yyjson数组类.枚举循环)
* **描述:** 只能在本类的"枚举循环"语句的子语句体中使用,用作取回当前所枚举到的成员值,如果有多层本类或本类继承类的"枚举循环"嵌套,本方法将返回最近层"枚举循环"所枚举到的成员值.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.到可读文本

* **类型:** `方法`
* **名称:** `到可读文本`
* **返回值数据类型:** `文本型`
* **参数:**
	* `选项` (`整数`, 默认值: `YYJSON格式化选项.缩进`): 请参考"YYJSON格式化选项"所提供的常量值,可使用其组合值.
* **描述:** 将当前数组以JSON格式化文本内容返回
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.插入成员

* **类型:** `方法`
* **名称:** `插入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `插入值` (`YYJSON对象类`): 提供所欲插入的成员,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 插入一个指定的对象成员到当前数组的指定索引位置
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.缓存并插入成员

* **类型:** `方法`
* **名称:** `缓存并插入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `插入值` (`YYJSON对象类`): 提供所欲插入的成员,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被插入一次,多次插入将导致成员值丢失或混乱.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 同"插入成员"方法,与其不同的是本方法参数的"插入值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.插入数组成员

* **类型:** `方法`
* **名称:** `插入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `插入值` (`YYJSON数组类`): 提供所欲插入的成员,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 插入一个指定的数组成员到当前数组的指定索引位置
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.缓存并插入数组成员

* **类型:** `方法`
* **名称:** `缓存并插入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `插入值` (`YYJSON数组类`): 提供所欲插入的成员,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被插入一次,多次插入将导致成员值丢失或混乱.
	* `索引` (`整数`): 提供所欲插入的索引位置
* **描述:** 同"插入数组成员"方法,与其不同的是本方法参数的"插入值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.插入成员到首部

* **类型:** `方法`
* **名称:** `插入成员到首部`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON对象类`): 提供所欲插入的成员,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
* **描述:** 插入一个指定的对象成员到当前数组的首部
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.缓存并插入成员到首部

* **类型:** `方法`
* **名称:** `缓存并插入成员到首部`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON对象类`): 提供所欲插入的成员,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被插入一次,多次插入将导致成员值丢失或混乱.
* **描述:** 同"插入成员到首部"方法,与其不同的是本方法参数的"添加值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.插入数组成员到首部

* **类型:** `方法`
* **名称:** `插入数组成员到首部`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON数组类`): 提供所欲插入的成员,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
* **描述:** 插入一个指定的数组成员到当前数组的首部
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.缓存并插入数组成员到首部

* **类型:** `方法`
* **名称:** `缓存并插入数组成员到首部`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON数组类`): 提供所欲插入的成员,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被插入一次,多次插入将导致成员值丢失或混乱.
* **描述:** 同"插入数组成员到首部"方法,与其不同的是本方法参数的"添加值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.替换成员

* **类型:** `方法`
* **名称:** `替换成员`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `索引` (`整数`): 提供所替换成员所处的索引位置
	* `替换值` (`YYJSON对象类`): 提供所欲替换的新值,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被替换一次,否则将出现成员值丢失的情况.
* **描述:** 将当前数组中指定的成员替换为一个新的值
* **返回值描述:** 返回被替换之前该索引位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.缓存并替换成员

* **类型:** `方法`
* **名称:** `缓存并替换成员`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `索引` (`整数`): 提供所替换成员所处的索引位置
	* `替换值` (`YYJSON对象类`): 提供所欲替换的新值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被替换一次,多次替换将导致成员值丢失或混乱.
* **描述:** 同"替换成员"方法,与其不同的是本方法参数的"替换值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 返回被替换之前该索引位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.替换数组成员

* **类型:** `方法`
* **名称:** `替换数组成员`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `索引` (`整数`): 提供所替换成员所处的索引位置
	* `替换值` (`YYJSON数组类`): 提供所欲替换的新值,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被替换一次,否则将出现成员值丢失的情况.
* **描述:** 将当前数组中指定的成员替换为一个新的值
* **返回值描述:** 返回被替换之前该索引位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.缓存并替换数组成员

* **类型:** `方法`
* **名称:** `缓存并替换数组成员`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `索引` (`整数`): 提供所替换成员所处的索引位置
	* `替换值` (`YYJSON数组类`): 提供所欲替换的新值,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被替换一次,多次替换将导致成员值丢失或混乱.
* **描述:** 同"替换数组成员"方法,与其不同的是本方法参数的"替换值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 返回被替换之前该索引位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.删除成员

* **类型:** `方法`
* **名称:** `删除成员`
* **返回值数据类型:** `YYJSON对象类`
* **参数:**
	* `索引` (`整数`): 提供所删除成员所处的索引位置
* **描述:** 删除当前数组中指定索引位置处的成员
* **返回值描述:** 返回被删除之前该索引位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.删除首部成员

* **类型:** `方法`
* **名称:** `删除首部成员`
* **返回值数据类型:** `YYJSON对象类`
* **描述:** 删除当前数组中处于首部的成员
* **返回值描述:** 返回被删除之前该位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.删除尾部成员

* **类型:** `方法`
* **名称:** `删除尾部成员`
* **返回值数据类型:** `YYJSON对象类`
* **描述:** 删除当前数组中处于首部的成员
* **返回值描述:** 返回被删除之前该位置处的成员值

##### 火山视窗.JSON支持.YYJSON数组类.删除范围成员

* **类型:** `方法`
* **名称:** `删除范围成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `起始索引` (`整数`): 提供所欲删除范围的起始索引位置
	* `删除数量` (`整数`): 提供所欲删除成员的数量
* **描述:** 删除当前数组中指定范围内的所有成员
* **返回值描述:** 返回是否删除成功

##### 火山视窗.JSON支持.YYJSON数组类.清空

* **类型:** `方法`
* **名称:** `清空`
* **返回值数据类型:** `逻辑型`
* **描述:** 清空当前数组中的所有成员
* **返回值描述:** 返回是否清空成功

##### 火山视窗.JSON支持.YYJSON数组类.清除缓存

* **类型:** `方法`
* **名称:** `清除缓存`
* **描述:** 在使用了"缓存并加入/插入"等系列方法后,如所被缓存对象不再需要使用(即充当当前对象的成员)时,可调用本方法清理缓存所占用内存.

##### 火山视窗.JSON支持.YYJSON数组类.翻转

* **类型:** `方法`
* **名称:** `翻转`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `次数` (`整数`): 提供所欲翻转的次数
* **描述:** 将当前数组中的成员翻转指定次数
* **返回值描述:** 返回是否翻转成功

##### 火山视窗.JSON支持.YYJSON数组类.加入新成员

* **类型:** `方法`
* **名称:** `加入新成员`
* **返回值数据类型:** `YYJSON对象类`
* **描述:** 插入一个指定的对象成员到当前数组的尾部
* **返回值描述:** 返回所插入的成员对象

##### 火山视窗.JSON支持.YYJSON数组类.加入新数组成员

* **类型:** `方法`
* **名称:** `加入新数组成员`
* **返回值数据类型:** `YYJSON数组类`
* **描述:** 插入一个指定的数组成员到当前数组的尾部
* **返回值描述:** 返回所插入的数组对象

##### 火山视窗.JSON支持.YYJSON数组类.加入成员

* **类型:** `方法`
* **名称:** `加入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON对象类`): 提供所欲插入的成员,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
* **描述:** 插入一个指定的对象成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.缓存并加入成员

* **类型:** `方法`
* **名称:** `缓存并加入成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON对象类`): 提供所欲插入的成员,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被加入一次,多次加入将导致成员值丢失或混乱.
* **描述:** 同"加入成员"方法,与其不同的是本方法参数的"添加值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.加入数组成员

* **类型:** `方法`
* **名称:** `加入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON数组类`): 提供所欲插入的成员,必须确保该对象的作用域大于或等于当前数组对象,且同一个值对象仅支持被加入一次,否则将出现成员值丢失的情况.
* **描述:** 插入一个指定的数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.缓存并加入数组成员

* **类型:** `方法`
* **名称:** `缓存并加入数组成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `添加值` (`YYJSON数组类`): 提供所欲插入的成员,该对象将会被缓存,如确保不再使用时可调用"清除缓存"方法释放所占用内存. 注意:同一个值对象仅支持被加入一次,多次加入将导致成员值丢失或混乱.
* **描述:** 同"加入数组成员"方法,与其不同的是本方法参数的"添加值"无需考虑其作用域,但执行效率较前者低.
* **返回值描述:** 插入成功返回真,否则返回假.

##### 火山视窗.JSON支持.YYJSON数组类.加入空成员

* **类型:** `方法`
* **名称:** `加入空成员`
* **返回值数据类型:** `逻辑型`
* **描述:** 插入一个空值作为数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.加入逻辑值成员

* **类型:** `方法`
* **名称:** `加入逻辑值成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`逻辑型`): 提供所欲加入的值
* **描述:** 插入一个指定的值作为数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.加入长整数成员

* **类型:** `方法`
* **名称:** `加入长整数成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`长整数`): 提供所欲加入的值
* **描述:** 插入一个指定的值作为数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.加入整数成员

* **类型:** `方法`
* **名称:** `加入整数成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`整数`): 提供所欲加入的值
* **描述:** 插入一个指定的值作为数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.加入小数成员

* **类型:** `方法`
* **名称:** `加入小数成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`小数`): 提供所欲加入的值
* **描述:** 插入一个指定的值作为数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.加入文本成员

* **类型:** `方法`
* **名称:** `加入文本成员`
* **返回值数据类型:** `逻辑型`
* **参数:**
	* `值` (`文本型`): 提供所欲加入的值
* **描述:** 插入一个指定的值作为数组成员到当前数组的尾部
* **返回值描述:** 插入成功返回真,否则返回假.
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON数组类.取展示内容

* **类型:** `方法`
* **名称:** `取展示内容`
* **参数:**
	* `展示内容` (`文本型`): 用作填入本对象中所有数据的展示文本
	* `最大展示数据尺寸` (`整数`): 提供用户所指定的最大允许展示数据尺寸,小于0表示全部展示,等于0表示展示默认尺寸数据.
* **特性:**
	* `虚拟方法`(可在继承类中覆盖)
* **描述:** 将本对象中的所有数据用文本方式填入到所提供的文本参数中,用作调试或其它场合展示时使用.

---

#### 火山视窗.JSON支持.YYJSON值类型

* **类型:** `类`
* **名称:** `YYJSON值类型`
* **特性:**
	* `常量类`(对应数据类型: `整数`)
* **文档分类:** `辅助类`
* **描述:** 表示一个JSON对象的类型

##### 火山视窗.JSON支持.YYJSON值类型.未知

* **类型:** `成员常量`
* **名称:** `未知`
* **数据类型:** `YYJSON值类型`

##### 火山视窗.JSON支持.YYJSON值类型.空值

* **类型:** `成员常量`
* **名称:** `空值`
* **数据类型:** `YYJSON值类型`

##### 火山视窗.JSON支持.YYJSON值类型.逻辑值

* **类型:** `成员常量`
* **名称:** `逻辑值`
* **数据类型:** `YYJSON值类型`

##### 火山视窗.JSON支持.YYJSON值类型.数值

* **类型:** `成员常量`
* **名称:** `数值`
* **数据类型:** `YYJSON值类型`

##### 火山视窗.JSON支持.YYJSON值类型.文本值

* **类型:** `成员常量`
* **名称:** `文本值`
* **数据类型:** `YYJSON值类型`

##### 火山视窗.JSON支持.YYJSON值类型.数组值

* **类型:** `成员常量`
* **名称:** `数组值`
* **数据类型:** `YYJSON值类型`

##### 火山视窗.JSON支持.YYJSON值类型.对象值

* **类型:** `成员常量`
* **名称:** `对象值`
* **数据类型:** `YYJSON值类型`

---

#### 火山视窗.JSON支持.YYJSON值子类型

* **类型:** `类`
* **名称:** `YYJSON值子类型`
* **特性:**
	* `常量类`(对应数据类型: `整数`)
* **文档分类:** `辅助类`
* **描述:** 表示一个JSON对象的子类型

##### 火山视窗.JSON支持.YYJSON值子类型.未知

* **类型:** `成员常量`
* **名称:** `未知`
* **数据类型:** `YYJSON值子类型`

##### 火山视窗.JSON支持.YYJSON值子类型.为假

* **类型:** `成员常量`
* **名称:** `为假`
* **数据类型:** `YYJSON值子类型`

##### 火山视窗.JSON支持.YYJSON值子类型.为真

* **类型:** `成员常量`
* **名称:** `为真`
* **数据类型:** `YYJSON值子类型`

##### 火山视窗.JSON支持.YYJSON值子类型.为无符号整数

* **类型:** `成员常量`
* **名称:** `为无符号整数`
* **数据类型:** `YYJSON值子类型`

##### 火山视窗.JSON支持.YYJSON值子类型.为有符号整数

* **类型:** `成员常量`
* **名称:** `为有符号整数`
* **数据类型:** `YYJSON值子类型`

##### 火山视窗.JSON支持.YYJSON值子类型.为小数

* **类型:** `成员常量`
* **名称:** `为小数`
* **数据类型:** `YYJSON值子类型`

---

#### 火山视窗.JSON支持.YYJSON格式化选项

* **类型:** `类`
* **名称:** `YYJSON格式化选项`
* **特性:**
	* `抽象类`(禁止直接定义其实例变量)
* **文档分类:** `辅助类`
* **描述:** 提供可使用的JSON格式化(到文本)时的可选项
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON格式化选项.压缩

* **类型:** `成员常量`
* **名称:** `压缩`
* **数据类型:** `整数`
* **描述:** 以最小占用的方式输出内容

##### 火山视窗.JSON支持.YYJSON格式化选项.缩进

* **类型:** `成员常量`
* **名称:** `缩进`
* **数据类型:** `整数`
* **描述:** 以4个空格符号缩进方式输出内容
* **相关例程:** [vol-yyjson数据访问-yyjson-main](#vol-yyjson数据访问-yyjson-main)

##### 火山视窗.JSON支持.YYJSON格式化选项.输出A码

* **类型:** `成员常量`
* **名称:** `输出A码`
* **数据类型:** `整数`
* **描述:** 将Unicode字符转义,仅输出ASCII码.

##### 火山视窗.JSON支持.YYJSON格式化选项.转义

* **类型:** `成员常量`
* **名称:** `转义`
* **数据类型:** `整数`
* **描述:** 将'/'转义为'\/'

##### 火山视窗.JSON支持.YYJSON格式化选项.字符表示无限

* **类型:** `成员常量`
* **名称:** `字符表示无限`
* **数据类型:** `整数`
* **描述:** 将inf或nan数字输出为"无限"和"nan"字符(非标准)

##### 火山视窗.JSON支持.YYJSON格式化选项.空表示无限

* **类型:** `成员常量`
* **名称:** `空表示无限`
* **数据类型:** `整数`
* **描述:** 将inf或nan数字输出为null
